@model IEnumerable<Ecommerce.Models.Order_preorder>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_layoutLogin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product_.productName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Customer_.userName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product_.productName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Customer_.userName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idOrder_preorder }) |
            @Html.ActionLink("Details", "Details", new { id=item.idOrder_preorder }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idOrder_preorder })
        </td>
    </tr>
}

</table>
